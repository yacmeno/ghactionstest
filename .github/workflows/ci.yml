on: [push]
jobs:
  test:
    runs-on: ubuntu-latest
    env:
      BRANCHNAME: ${{ github.ref_name }}
    steps:
      - name: script
        run: |
          echo "BRANCH NAME: $BRANCHNAME"
          sleep 60
        shell: bash