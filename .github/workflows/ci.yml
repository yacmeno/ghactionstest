on: [pull_request]
jobs:
  test:
    runs-on: ubuntu-latest
    env:
      TEST: ${{ github.ref_name }}
    steps:
      - name: script
        run: |
          echo "PR author: $TEST"
        shell: bash